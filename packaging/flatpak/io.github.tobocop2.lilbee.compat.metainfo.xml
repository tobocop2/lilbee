<?xml version="1.0" encoding="UTF-8"?>
<component type="console-application">
  <id>io.github.tobocop2.lilbee.compat</id>
  <name>lilbee-compat</name>
  <summary>The whole local AI stack in one executable (pre-Haswell CPU build)</summary>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>MIT</project_license>
  <description>
    <p>
      lilbee is the whole local AI stack in one executable: it runs and manages
      local AI models across every GPU you have, indexes your files and code,
      crawls the web pages you point it at, and answers questions with
      citations down to the file and line. One install covers the TUI, CLI,
      HTTP server, and MCP server. You don't need Ollama or LM Studio, and it
      works with both.
    </p>
    <p>
      This build runs on older x86_64 CPUs (Sandy Bridge / Bulldozer class)
      that crash on the stock build's lancedb wheel.
    </p>
  </description>
  <url type="homepage">https://lilbee.sh</url>
  <url type="bugtracker">https://github.com/tobocop2/lilbee/issues</url>
  <provides>
    <binary>lilbee</binary>
  </provides>
</component>
