SearchHFCtaItem {
    height: auto;
    padding: 0 1;
    margin: 1 0 0 0;
    border-left: $rail-heavy transparent;
    pointer: pointer;
    color: $accent;
    text-style: bold;

    &:hover {
        background: $surface;
    }

    &:focus {
        border-left: $rail-heavy $accent;
        background: $surface;
    }

    #cta-label {
        width: 1fr;
        height: 1;
    }
}
