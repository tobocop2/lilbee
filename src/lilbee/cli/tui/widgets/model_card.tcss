ModelCard {
    height: auto;
    border: $rail $secondary 50%;
    padding: 0 1;
    pointer: pointer;

    &:hover {
        background: $surface;
        border: $rail $accent;
    }

    /* Mirror the list-view highlight: background fill plus accent border
     * so the focused card reads as a clearly separated, selected tile. */
    &.-highlight {
        background: $primary 30%;
        border: $rail $accent;
    }

    &:focus, &:focus-within {
        background: $primary 30%;
        border: $rail $accent;
    }

    .card-body {
        width: 1fr;
    }
}
